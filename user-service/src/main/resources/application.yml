server:
  port: 9090



spring:
  datasource:
    driver-class-name: org.h2.Driver
    hikari:
      jdbc-url: jdbc:h2:mem:testdb
      username: sa
      password:

jwt:
  issuer : jara
  subject : auth
  expires-time : 3600 # 1시간
  secret : my-secret